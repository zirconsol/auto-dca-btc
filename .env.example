# Copia este archivo a ".env" y completa tus credenciales reales.
BINANCE_API_KEY=tu_api_key
BINANCE_API_SECRET=tu_api_secret

# Activo de cotización a vigilar (por defecto ARS).
TARGET_ASSET=ARS

# Par spot a usar para comprar BTC con ese activo (por defecto BTCARS).
TRADE_SYMBOL=BTCARS

# Intervalo de consulta en segundos (por defecto 10).
POLL_INTERVAL_SECONDS=10

# Mínimo en moneda de cotización para disparar la orden (0 para sin mínimo).
# MIN_QUOTE_QTY=0

# Endpoint de Binance (por defecto prod). Cambia a testnet si usas claves de test.
# BINANCE_BASE_URL=https://testnet.binance.vision

# Parámetros para retiros manuales (ver src/withdraw_btc_bnb.py)
# WITHDRAW_COIN=BTC
# WITHDRAW_AMOUNT=0.000001
# WITHDRAW_ADDRESS=0xwithdrawwallet
# WITHDRAW_NETWORK=BSC

# Backend (FastAPI) para dashboard
# DCA_DB_URL=sqlite:///./dca.db
# DCA_PRICE_SYMBOL=BTCARS
# DCA_PRICE_BASE_URL=https://api.binance.com

# Frontend
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# NEXT_PUBLIC_MANUAL_WITHDRAW_ADDRESS=0xDestinoManualParaRetiro

# Backend (FastAPI) para dashboard
# DCA_DB_URL=sqlite:///./dca.db
# DCA_PRICE_SYMBOL=BTCARS
